# FM of the Future — Guided Demo Walkthrough (screenshots)

Captured live from the running app on 2026-06-10 (viewport 1440×900). Each image maps to a
storyboard shot from `docs/fm-future-storyboard.xlsx` (see `docs/storyboard-alignment.md`).
This is the exact product UI that gets screen-recorded for the demo.

| File | Shot(s) | What it shows |
|---|---|---|
| `shot-04-overview-hero.png` | 4 | Overview hero — "one product · two capabilities", Start guided demo, exec readiness tiles, pipeline. |
| `shot-05-pipeline-boundary.png` | 5 | The pipeline inside the labeled "Financial Management of the Future" product boundary; the record token travels reconcile→package→deliver; Auditor node sits outside the frame. |
| `shot-06-07-recon-sources.png` | 6, 7 | Reconciliation title card + the source datasets (Purchase Orders / Goods Receipts / General Ledger), ready to run. |
| `shot-07-recon-result.png` | 7 | Job complete — 68 reconciled / 20 escalated exceptions ("review 20 exceptions, not 88 rows"). |
| `shot-08-exceptions.png` | 8 | Review queue — exceptions only; amber escalations; the agent's "why" rationale expanded on the first row. |
| `shot-09-audit-ready-toast.png` | 9 | Audit-ready output with Export / Share affordances and the "✓ Reconciliation exported" toast; hands the record to PBC. |
| `shot-10-11-pbc-intake.png` | 10, 11 | PBC title card + the "Submit a request" compose card (operator types a request → submit). |
| `shot-12-reasoning-sources.png` | 12 | Agent reasoning / provenance trail + the Retrieved sources cards (PO-23577 / DOC-795613 / GL-00043) with the "Every record traceable" chip. |
| `shot-13-package.png` | 13 | Auditor-ready package — DEFENSIBLE, "~3 days → minutes" (confirmed/cited), SFA chip, three-way tie, line-level provenance. |
| `shot-14-lineage.png` | 14 | End-to-end lineage: one record threaded through the boundary frame (reconcile→package→deliver), auditor outside. |
| `shot-15-16-18-strips.png` | 15, 16, 18 | "In production today" missions strip; "Built on accredited building blocks" (Databricks · Cloud-native · Agentic orchestration · HITL); subscription / contact-for-a-quote. |

**Not pictured (produced video, not product UI):** Shots 1–3 (cold open + b-roll), Shot 17
(TRL/SBIR pitch badges), Shot 19 (CTA/QR), Shot 20 (final logo lockup — the Acon is visible in
every header here).

Notes: Shot 7's progress bar is a live ~2s animation (not freezable in a static frame), so it's
represented by the loaded-sources and completed-result captures. The Shot 9 toast is a brief
2.4s confirmation, captured here by momentarily holding it open.
